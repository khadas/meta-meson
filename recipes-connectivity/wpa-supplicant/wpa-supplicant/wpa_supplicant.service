[Unit]
Description=WPA supplicant
Before=network.target
Wants=network.target
After=wifi.service

[Service]
Type=oneshot
ExecStart=/usr/sbin/wpa_supplicant -u -B -Dnl80211 -iwlan0 -c /etc/wpa_supplicant.conf
RemainAfterExit=yes
Restart=on-failure
RestartSec=1

[Install]
WantedBy=multi-user.target
