diff --git a/ext/libav/gstavdemux.c b/ext/libav/gstavdemux.c
index 7ba2544..b816e64 100644
--- a/ext/libav/gstavdemux.c
+++ b/ext/libav/gstavdemux.c
@@ -2089,13 +2089,8 @@ gst_ffmpegdemux_register (GstPlugin * plugin)
         !strcmp (in_plugin->name, "ivf"))
       register_typefind_func = FALSE;
 
-    env = getenv("ENABLE_FFMPEG_DEMUX");
-    if ((env) && (!strcmp(env, "TRUE"))) {
-		if(!strcmp(in_plugin->name, "mpegts"))
-			rank = GST_RANK_NONE;
-		else
-            rank = GST_RANK_PRIMARY;
-    } else {
+    env = getenv("VENDOR_MEDIA_GST_LIBAV_ENABLE_FFMPEG");
+    if ((env) && (!atoi(env))) {
         /* Set the rank of demuxers known to work to MARGINAL.
          * Set demuxers for which we already have another implementation to NONE
          * Set All others to NONE*/
@@ -2142,6 +2137,11 @@ gst_ffmpegdemux_register (GstPlugin * plugin)
           rank = GST_RANK_NONE;
           continue;
         }
+    }else {
+        if(!strcmp(in_plugin->name, "mpegts"))
+            rank = GST_RANK_NONE;
+        else
+            rank = GST_RANK_PRIMARY;
     }
 
     /* construct the type */
