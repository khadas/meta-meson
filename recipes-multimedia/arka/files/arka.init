#!/bin/sh

# Starts arka service.
start() {
	printf "Starting arka service: \n"

	/usr/bin/Arka &
}

stop() {
	printf "Stopping arka service: \n"
}

case "$1" in
  start)
	start
	;;
  stop)
	stop
	;;
  *)
	echo "Usage: $0 {start|stop}"
	exit 1
esac

exit $?

